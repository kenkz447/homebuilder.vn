/**
 * Base on Bootstrap breakpoint
 */
export const BreakPointSizes: Array<number> = [
    1200,
    992,
    768,
    576,
    0
]

export enum LayoutBreakPoints {
    xl = BreakPointSizes[0],
    lg = BreakPointSizes[1],
    md = BreakPointSizes[2],
    sm = BreakPointSizes[3],
    xs = BreakPointSizes[4]
}